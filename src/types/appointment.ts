export interface ItemType {
  id: number,
  name: string
}

export interface DoctorType {
  id: number,
  name: string,
  description: string,
  image: string
}

export interface DateType {
  id: number,
  time: string
}